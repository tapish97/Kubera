"use client";
import { useMemo, useState } from "react";

export function SearchSelect({ value, onChange, options, placeholder, label }: { value: string; onChange: (value: string) => void; options: string[]; placeholder: string; label: string }) {
  const [open, setOpen] = useState(false); const matches = useMemo(() => options.filter((option) => option.toLocaleLowerCase().includes(value.trim().toLocaleLowerCase())).slice(0, 8), [options, value]);
  return <div className="relative mt-2"><div className="flex min-h-14 overflow-hidden rounded-2xl border border-[#d8d2c6] bg-white focus-within:border-[#216148]"><input value={value} onChange={(e) => { onChange(e.target.value); setOpen(true); }} onFocus={() => setOpen(true)} placeholder={placeholder} aria-label={label} autoComplete="off" className="min-w-0 flex-1 px-4 text-base text-[#20241f] outline-none"/><button type="button" onClick={() => setOpen(!open)} aria-label={`Show ${label} choices`} className="grid w-12 place-items-center border-l border-[#eee9df] text-[#216148]"><svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" className={`h-5 w-5 transition ${open ? "rotate-180" : ""}`}><path d="m6 9 6 6 6-6"/></svg></button></div>{open && matches.length > 0 && <div role="listbox" className="absolute z-20 mt-1 max-h-64 w-full overflow-auto rounded-2xl border border-[#ded8cc] bg-white p-1 shadow-xl">{matches.map((option) => <button key={option} type="button" role="option" aria-selected={option === value} onMouseDown={(e) => e.preventDefault()} onClick={() => { onChange(option); setOpen(false); }} className="min-h-12 w-full rounded-xl px-4 text-left font-semibold text-[#20241f] hover:bg-[#edf4ef]">{option}</button>)}</div>}</div>;
}
